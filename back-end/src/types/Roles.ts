/**
 * @enum Roles
 * @description Enum for user roles
 * @property {string} ADMIN - Admin role
 * @property {string} TRAINER - Trainer role
 * @property {string} CLIENT - Client role
 */
enum Roles {
  ADMIN = 'admin',
  TRAINER = 'trainer',
  CLIENT = 'client',
}

export default Roles;
