/**
 * @interface AppConfig
 * @description App configuration interface for MongoDB URI
 * @property {string} MONGO_URI - MongoDB URI
 */
interface AppConfig {
  MONGO_URI: string;
}

export default AppConfig;
