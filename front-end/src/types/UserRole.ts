/**
 * @enum UserRole
 * @description The user role enumeration.
 * @property {string} Admin
 * @property {string} Trainer
 * @property {string} Client
 */
enum UserRole {
  Admin = 'admin',
  Trainer = 'trainer',
  Client = 'client',
}

export default UserRole;
