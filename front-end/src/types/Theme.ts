/**
 * @interface Colors - for the theme colors.
 * @property {string} primaryColor
 * @property {string} secondaryColor
 * @property {string} backgroundColor
 */
export interface Colors {
  primaryColor: string;
  secondaryColor: string;
  backgroundColor: string;
}
