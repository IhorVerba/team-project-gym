import LoginForm from '../components/LoginForm/LoginForm';

/**
 * @function LoginPage
 * @description This component of page is used to show login page.
 * @see {@link LoginForm} component
 * @returns LoginPage component
 */
const LoginPage = () => {
  return <LoginForm />;
};

export default LoginPage;
