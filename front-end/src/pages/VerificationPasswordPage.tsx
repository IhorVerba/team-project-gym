import { VerificationPassword } from '../components/VerificationPassword/VerificationPassword';

/**
 * @function VerificationPasswordPage
 * @description This component of page is used to show verification password page.
 * @see {@link VerificationPassword} component
 * @returns VerificationPasswordPage component
 */
export const VerificationPasswordPage = () => {
  return <VerificationPassword />;
};
